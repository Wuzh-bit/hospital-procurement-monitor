---
name: hospital-procurement-monitor
version: 1.0.0
description: 监测中国大陆目标医院的公开采购、需求调查、市场调研、参数征集和采购意向等线索，并维护可追溯的本地台账。用户提出建立、运行、调整医院采购监测任务，导入医院清单，确认医院和信息源，筛选采购公告，导出线索表或创建 WorkBuddy 定时任务时使用。
---

# 医院采购线索监测

监测中国大陆无需登录的公开采购信息。**分工铁律：Python 负责一切确定性工作（采集、实测验证、去重、初筛、详情摘要），AI 只做四件轻量的事——确认、单次搜索、读压缩摘要审阅候选、呈现结果。** 页面级爬取与“逐页实测”一律交给脚本，AI 不做，这是控制 Token 与避免推理死循环的根本手段。

## 全局规则（每次执行先读）

| # | 规则 | 适用范围 |
|---|------|----------|
| 1 | 信息源整理（含搜索与实测）只在首次建立与中途调整时进行；每次运行直接用配置，**零搜索** | 全部 |
| 2 | 每家医院信息源固定两类：**官网公告栏目 + 所在省/市政府采购或公共资源交易平台**；不自动发现代理机构来源 | 首次建立、中途调整 |
| 3 | WebSearch 一律“**单次查询、必有结论**”：一次查询取前 5 条结果即定稿或标“人工关注”，禁止追加查询、禁止递归搜索 | 首次建立、补盲 |
| 4 | 首次建立整轮搜索预算：每家医院至多 1 次（官网主页，用户已提供则 0 次），总次数 ≤ 医院数 + 3；超出即改用预置平台或请用户提供地址 | 首次建立 |
| 5 | 实测验证全部由 `discover_sources.py` 完成，AI 只读其输出的紧凑 JSON 并转成表格，不做任何页面实测 | 首次建立、中途调整 |
| 6 | 不绕过登录、验证码、付费墙或反爬；JS 检索页/需登录/TLS 异常一律标“人工关注”，不逆向接口、不模拟浏览器 | 全部 |
| 7 | 候选审阅两级制：标题级初判 → 仅对相关/存疑候选看压缩摘要；同一候选至多打开一次链接 | 每次运行 |
| 8 | 来源失败不提高请求频率、不 AI 重试搜索，只记录原因并继续其他来源 | 全部 |

## 安全与边界

- 仅访问用户确认、无需登录的公开 `http/https` 来源，遵守站点规则与频率。TLS/证书错误给出中文诊断（可改配 http 入口或标“人工关注”），不自动降级校验。
- **JS 页面渲染（`sources[].mode: "js"`）**：对公开、无需登录/验证码的 JS 列表页（如中国政府采购网、部分省级平台）用浏览器无头渲染后采集。**已集成 Playwright（可选依赖，优先）** + 系统浏览器 dump-dom 兜底；验证可解锁 ccgp 国家级政采公告及北京/河南/江苏/海南等省平台（TLS/JS 类）。仅渲染公开页面、使用合理 UA、不填表、不点验证码、不绕过登录/访问控制；限速不变；用完即关；渲染失败仍标“人工关注”。Playwright 为可选依赖（`pip install playwright`，复用系统 Chrome/Edge，无需下载浏览器），未安装时自动退回系统浏览器渲染。**Safari 不支持命令行无头渲染**，仅 Safari 的电脑需装 Chrome 或改用静态来源。
- 不收集或保存密码、密钥、Cookie、令牌；只有已连接邮件工具可用时才发结果邮件。
- 配置、缓存、数据库和报告存用户指定的独立数据目录（如 `monitor-data/`），不写入本 skill 目录或 Git 仓库。
- 清理缓存必须先预览，仅在用户确认或自动化已授权时加 `--apply`。
- 不把“历史合作代理机构”“医院简称映射”当事实；给候选和证据后等用户确认。

## 首次建立监测（轻量流程）

**无既有配置的首次启动，完成以下确认后才允许写入配置、启用来源、创建定时任务；每步都在用户确认后写入。任何一步未确认不得推进。**

| 步骤 | 做什么 | 确认方式 | 搜索预算 |
|------|--------|----------|----------|
| 1 周期与邮箱 | 确认运行周期（每日/每周等）、时区、通知邮箱；明示每轮新增且 AI 判相关的线索会附 Excel/CSV 报表发送。邮件工具不可用则先按“仅本地报告”运行 | 用户回复确认 | 0 |
| 2 医院清单 | 接受聊天文本、CSV/XLSX、DOCX 中的医院名单；**仅对简称/口语名（如“市一”“省中医”）或缺省市的医院做单次搜索确认正式名称**；正式全名+省市已齐全的医院不搜索。处理同名、院区、旧称更名 | 批量：“全部确认”或逐条指出；未确认的保持 `confirmed: false` | 仅歧义医院，每家 ≤1 次 |
| 3 信息源 | 见下方“信息源整理三步” | 批量：可用性评估表一次列出，回复“全部确认”即启用所有“可用”来源 | 每家官网主页 ≤1 次，仅未提供时 |
| 4 项目类型与关键词 | **仅请用户提供项目类型词**（如“互联网医院”“医院信息化”）；AI 按 [关键词扩展](references/keyword-extension.md) 自动生成同义词、排除词、优先公告类型候选，一次性列出请用户确认 | 批量：“全部确认”或指出增删；未确认不写入配置 | 总计 ≤1 次（仅对行业表达无把握时） |
| 5 写入配置 | 复制 `templates/monitor-config.example.json` 到数据目录，填充已确认内容；不启用示例来源或邮箱 | — | 0 |
| 6 轻量试运行 | 完整跑一轮（`--since` 建议取近 7 天），展示候选数、排除理由、通知路径、报告位置 | 用户确认无误后再创建定时任务 | 0 |

### 信息源整理三步（步骤 3 细节）

**第 1 步：收集官网地址（批量自动，用户零操作）。** 医院较多时不需要用户逐个整理官网地址，按以下顺序自动补齐 homepage：
1. **（可选加速，零 AI 搜索）POI 批量查询**：配置了高德 Web 服务 Key（免费申请 https://lbs.amap.com ，环境变量 `AMAP_KEY`）时，运行 `scripts/lookup_homepages.py --hospitals <医院清单.json> --output <补全后.json>`，按医院全名批量查官网并填回 `homepage`。
2. **（默认通道，零配置）AI 批量搜索**：没有 Key 时，AI 对 homepage 仍为空的医院逐家至多 1 次搜索（`"{正式名称}" 官网`，单次查询必有结论），把结果填入清单后重跑脚本。搜不到或域名存疑就留空标“官网未知”，该医院以省平台为来源，不再追加搜索。

用户手动提供官网地址只是“零搜索”的可选项，不是必须步骤。

**第 2 步：脚本实测（Python，零 Token、零推理）。** 把医院清单（含 homepage）写成 JSON 后运行：

```bash
python scripts/discover_sources.py --hospitals <医院清单.json> --data-dir <data-dir>
```

输入结构见 `templates/hospitals-input.example.json`。脚本自动完成：
- 抓官网主页、下钻 1 层找“公告/通知/招标/采购/公示/信息公开”栏目链接（每家 ≤3 个）；
- 按预置表 `references/gov-platforms.json` 实测所在省/市政府采购与公共资源交易平台（表未覆盖的省份回退国家级平台 ccgp.gov.cn / ggzy.gov.cn）：**先抓平台首页（门户）并下钻 1 层找“采购公告/交易信息/结果公告”等静态栏目页**——政采平台新版多为 JS 门户，老式平台常有静态栏目列表，能定位到的栏目连同首页一起实测；另轻量探测站点自带的 GET 搜索表单（`?keyword=xx`）并在评估输出中标出；
- 对每个候选输出实测结果：可达性、静态可解析、列表条目数、标题日期可读性、分页线索样本，写入 `work/source-assessment-<时间戳>.json`；**渲染探测默认关闭，需要时用 `--js-probe` 开启**（已集成 Playwright），对静态不可用但可访问的候选做渲染探测，渲染后能解析出带日期公告列表的标记 `js_render_usable`（建议按 `mode: "js"` 启用）。

脚本不搜索、不翻全站、不推理；每家的请求数 ≤ 9（另加静态不可用候选的渲染探测）。**JS 检索页/需登录的平台渲染后仍不可采则标“人工关注”**，不逆向接口、不绕过访问控制。

**第 3 步：AI 读表 + 用户批量确认（异常优先，只报全挂医院）。** AI 只读评估 JSON（每家医院官网候选 ≤3 条 + 省平台候选 ≤4 条含下钻栏目），按以下方式呈现：

- **先一行列出每家医院是否可启用**：`可启用来源 n 个`（含官网/省平台；仅当用 `--js-probe` 时才会出现"JS 渲染可采"标记）；**只有"所有候选都不可用"的全挂医院才列明细表**（说明原因与可补方向：补官网地址 / 换属地其他平台 / 标人工关注）；
- 用户回复"全部确认"→ 所有 `has_usable_source=true` 的医院全部启用其可用来源，无需逐条确认；全挂医院单独处理；
- 若需启用 JS 渲染来源，先跑 `--js-probe` 探测（已集成 Playwright），确认某站点 `js_render_usable` 且实测可采后，再按 `mode: "js"` 启用该来源（预置表 `js_ok` 状态的平台可直接按 `mode: "js"` 启用）。

**双来源默认同启用（交叉验证）**：某医院官网与属地公共资源交易平台**同时可用时，默认两个都启用、每轮都抓取**——同一公告在两个来源出现即交叉验证，可信度更高，且互相兜底。审阅时对跨来源标题一致的候选**自动合并为一条**（详见审阅提示词）。

**启用规则**：
- 同一医院出现多个同名栏目（如多个“通知公告”）时只保留主栏目；
- 分页线索由脚本给出原始链接样本，用户确认后配置 `pagination` 模板（见 [配置字段](references/configuration.md)）；
- **公告量巨大的政采/公共资源平台来源，启用时一并确认关键词定向**（`keyword_filter` + `keyword_early_stop_pages`，见 [配置字段](references/configuration.md)）。

只有**实测可用且用户确认**的来源写入配置 `sources` 持久保存。此后每次运行直接调用配置，零搜索、零实测。

### 项目类型与关键词（步骤 4 细节）

**只向用户要一个“项目类型”词**，例如“互联网医院”“医院信息化”“医疗设备”“检验试剂”。不要要求用户提供同义词、排除词或公告类型——这些由 AI 生成：

1. 根据项目类型词，按 `references/keyword-extension.md` 的规则生成候选：
   - `synonyms`：同业务语义的中文常见表达（必要时 ≤1 次总搜索确认行业叫法）；
   - `exclude`：同音/近义但业务无关的干扰词（如“互联网医院”→“互联网宽带”）；
   - `priority_notice_types`：默认采用需求侧早介入信号（需求调查/市场调研/参数征集/采购意向等）；
   - `noise_link_texts`：默认栏目噪音集，来源确认时可按实际页面补充。
2. **一次性列表请用户批量确认**：回复“全部确认”即全部采纳；用户指出增删（如“加 XX、去掉 YY”）后更新；未确认不写入配置。
3. 约束：**不跨品类扩展**——项目类型是“互联网医院”就不能扩出“医疗设备采购”；宁可少而准，后续按误报/漏报再调。

### 首次试运行（轻量）

- 用 `--since` 限定近 7 天缩小回看窗口，减少首轮数据量；
- 跑 `collect → prepare → fetch → 审阅 → record`，向用户展示候选数与排除理由、报告路径；
- 用户确认无误后再创建自动化任务。

## 每次监测运行

生成固定 `RUN_ID`（如 `20260805-090000`），依序执行：

```text
collect_notices.py → prepare_candidates.py → fetch_details.py → AI 审阅 → record_leads.py →（按保留策略清理）
```

### 1. 受控采集

```bash
python scripts/collect_notices.py --config <data-dir>/monitor-config.json --data-dir <data-dir> --run-id <RUN_ID> --since <上次成功运行日期>
```

只处理已启用且已确认的来源，来源间与翻页请求至少间隔 1 秒。`--since` 过滤可识别日期早于区间的公告；未读出日期的保留给审阅，避免误删。配置 `pagination` 的来源按模板翻页，整页早于 `--since` 时提前停止。**`mode: "js"` 的来源用系统浏览器无头渲染后采集**（自动探测 Chrome/Edge/Firefox，仅公开页面，限速不变）。**配置了 `keyword_filter` 的来源只保留标题命中关键词的公告（未命中直接丢弃并计数），连续零命中达到 `keyword_early_stop_pages` 即停止翻页**——用于公告量巨大的政采/公共资源平台（见 [配置字段](references/configuration.md)）。结果写 `raw/<RUN_ID>/`：退出码 0 全部成功，3 为完成但有来源失败（须报告）；`warnings` 质量告警（JS 渲染、链接全被过滤、疑似导航页）也要说明。单来源失败不中断其他来源。

### 2. 初筛与去重

```bash
python scripts/prepare_candidates.py --config <data-dir>/monitor-config.json --data-dir <data-dir> --run-id <RUN_ID>
```

按确认的关键词/同义词/排除词/公告类型输出 `work/candidates-<RUN_ID>.json`；跳过已入台账、已判不相关、及 canonical URL 与既有记录或本批候选重复的公告（同 URL 只留首条），计数写入输出。未命中/被排除原因保留在输出中供追踪复核，已排除记录不进审阅。

### 3. 详情摘要与审阅（两级，控制 token 与耗时）

先批量抓取候选详情摘要，再两级审阅：

```bash
python scripts/fetch_details.py --data-dir <data-dir> --run-id <RUN_ID>
```

`fetch_details.py` 用 Python 批量抓候选详情页，提取标题、发布时间、预算、截止日期、正文前 300 字，输出 `work/details-<RUN_ID>.json`；**网络请求与页面正文都不进入 LLM 上下文**。单条失败不中断，失败原因记录在结果中。

**第 1 级——标题级批量初判（零网络）**：读取 `candidates` 数组全部候选，仅凭标题、来源、链接、`evidence`（命中词/截断标记）逐条初判：
- 明显相关（标题即命中项目词）→ 预判 `relevant: true`；
- 明显不相关（标题即无关品类）→ 预判 `relevant: false`；
- 存疑（命中词但需正文确认，或 `evidence.title_truncated` 为 true）→ 标记待看详情。

**第 2 级——只对“预判相关/存疑”的候选看详情**：优先读 `work/details-<RUN_ID>.json` 的压缩摘要（标题、发布时间、预算、截止日、正文前 300 字），确认后定稿；摘要信息不足时再打开原链接。**明确不相关的候选不打开任何链接；同一候选至多打开一次，无法从摘要判断时宁可标“存疑”附理由，不要反复抓取。**

**跨来源合并（双来源交叉验证）**：同一公告同时出现在官网与省/市公共资源平台（标题去空格/标点后一致）时，**只输出一条**：取较权威来源（官网优先）的候选，`ai_reason` 注明“双来源交叉验证（官网 + 省平台）”，另一条候选输出 `relevant: false` 并在 `ai_reason` 写“已并入双来源交叉验证（见同标题官网/平台条目）”，避免台账与报告重复。

输出约束（控制 token）：
- `ai_reason` 限 60~80 字，说明命中证据与判断；不相关候选一句话（≤30 字）。
- `notice_type`/`project_type` 仅对相关候选填写；不相关候选只写 `relevant: false` + `ai_reason`。
- `budget`/`deadline` 只在 `details` 中已提取或你已确认时填写，抓不到就留空，不要为填字段额外抓取。
- 每个候选都要有结论（避免重复送审）；`notice_id` 必须完整复制自 candidates（64 位，勿截断）。

审阅提示词模板：

```text
读取 work/candidates-<RUN_ID>.json 与 work/details-<RUN_ID>.json，逐条审阅 candidates 数组：
1) 第1级按标题/来源/命中词初判，标记 相关/不相关/存疑；
2) 第2级只对 相关/存疑 的候选核对 details 摘要（预算/截止日/完整标题），摘要不足再开原链；
3) 跨来源合并：标题去空格/标点后一致的候选视为同一条公告（双来源交叉验证），只输出较权威来源一条，
   ai_reason 注明“双来源交叉验证（官网+省平台）”；另一条标记 relevant:false 并注明已并入；
4) 按 lead-annotations.example.json 格式输出 JSON 数组；ai_reason 限 60~80 字，不相关候选一句话理由；
5) notice_id 完整复制自 candidates，不得截断；每个候选都必须有结论。
6) 凡在通知/报告/呈现中引用公告链接，必须逐字复制 candidates/details 中的 `url` 字段，禁止手写、猜测、缩写或截断；链接以采集数据为准，不得以记忆或推断补全。
```

覆盖多院的来源（如政采平台）可用 `hospital_name` 覆盖 source 绑定的医院。

### 4. 入库与导出

```bash
python scripts/record_leads.py --data-dir <data-dir> --candidates <data-dir>/work/candidates-<RUN_ID>.json --annotations <data-dir>/work/annotations-<RUN_ID>.json
```

永久保存到 `monitor.sqlite3`，导出 `reports/procurement-leads.csv`；装可选 `openpyxl` 后同时导出 `.xlsx`。导出表头均为中文：医院名称、公告标题、公告类型、项目类型、命中关键词、智能判断依据、发布日期、信息来源、公告链接、预算/截止日期、状态、备注。`skipped_notices` 表保存判不相关/未标注的候选，不进入线索报告；复审时先从该表删除再重跑。入库按 canonical URL 去重；已入库线索可用 `"update": true` 更新字段，无需重采。

### 5. 提醒与清理

- 对本轮**新增**且 AI 判相关的线索，已连邮件工具则将 Excel/CSV 作为附件发送，正文列出数量、医院、标题、链接（链接必须逐字取自 candidates/details 的 `url` 字段，禁止手写、猜测或截断）；否则告知报告本地路径。不发送无关抓取内容。
- 原始缓存默认保留 30 天，先预览再确认：

```bash
python scripts/cleanup_cache.py --data-dir <data-dir> --older-than-days 30
```

确认后才加 `--apply`；台账不会被删除。

## AI 搜索补盲（可选，低频）

Python 采集覆盖“已确认的静态来源”。对无官网、仅发布在 JS 检索平台、TLS 异常不可采集的医院，或连续多轮零结果的来源，可低频抽查：

- **频率与刹车**：每周至多 1 轮；每轮至多 5 家医院；每家至多 1 个检索词（`医院名 + (项目关键词 | 招标 | 采购 | 公告 | 调研)`）；**单次查询、前 5 条结果即定稿，命中即停，不再细化查询**。
- **用途**：发现新来源（按信息源准入确认后补充）、验证漏报、补充采集不到的公告。
- 搜到候选按常规流程记入台账（注明“AI 搜索补充”）；AI 搜索仅作低频抽查与补漏，不作常规采集手段，以免成本上升、结果不可追溯。

## WorkBuddy 自动化

先手动跑通一次并确认报告附件正常。创建自动化任务时选择本 skill、同一工作目录和所需周期。任务提示词应要求：读取既有配置、执行本流程、只向本轮新增且 AI 判相关的线索发通知、保留失败日志，且不修改医院、关键词或来源，除非获得用户新的明确确认。**自动化执行中不做任何搜索与信息源发现。**

推荐任务提示词：

```text
使用 hospital-procurement-monitor 运行已确认的医院采购线索监测。读取现有配置，采集确认来源，规则初筛后审阅候选，更新本地台账并导出报告。仅对本次新增且 AI 判为相关的线索发送已连接邮箱通知（通知正文/报告中的公告链接必须逐字取自 candidates/details 的 `url` 字段，禁止手填或截断）；若邮件不可用，保留报告并说明原因。不得修改医院、关键词、来源或通知收件人；遇到验证码、登录或采集失败时记录原因并继续其他来源。
```

先低频试运行，并在任务设置中启用结果推送。

## 调整与维护

- **医院清单增减（中途调整）**：
  - **新增医院（含批量）**：只对新医院执行“名称确认 → 来源建议 → 用户确认”的增量流程（复用信息源整理三步）：把新医院列表写成 JSON →（可选）`lookup_homepages.py` 批量补官网，或由 AI 逐家至多 1 次搜索补齐 → `discover_sources.py` 实测 → 可用性评估表一次列出、用户回复“全部确认”即批量写入配置 `hospitals` 与 `sources`。既有医院、关键词和来源一律不动。批量新增时用户不需要手动整理官网地址。
  - **停用/删除医院**：将配置中该医院改为 `confirmed: false`（或其来源 `enabled: false`），其来源随之停用；**台账中已入库的历史线索保留**，仅停止后续采集。
  - **更名/别名更新**：更新 `official_name` / `aliases`，历史记录保留。
  - 调整后应手动运行一次验证配置生效，再继续自动化任务。
- 多页来源：优先为单一来源配置 `pagination`（URL 模板 + 最大页数），不要每页注册成独立来源；`--since` 控制回看窗口并支持翻页提前停止。offset 步长分页用 `step`、旧页在前的递减分页用 `descending`（详见 [配置字段](references/configuration.md)）。
- 调项目范围：先展示拟增删关键词及影响，确认后更新 `keywords`；不静默改写规则。
- 加来源：按信息源准入确认后启用；连续失败的来源暂停并提示复核，不反复高频请求。
- 误报/漏报：在备注记录样本与原因，优先调整同义词、排除词、公告类型规则；不用单条人工结论替代通用规则。

## 关键约束重申（三明治）

- **分工铁律**：AI 只做确认、单次搜索、读压缩摘要审阅、呈现结果；采集、实测、去重、初筛、摘要全部由 Python 完成。
- **搜索刹车**：任何 WebSearch 都是“单次查询、必有结论”；失败或信息不足一律标“人工关注”或改用预置平台，绝不追加查询、绝不递归搜索。
